{
	"model": {
		"type": "minecraft:select",
		"property": "minecraft:display_context",
		"cases":
		[{
			"when": [ "gui", "ground", "fixed", "on_shelf" ],
			"model": { "type":"model", "model":"${INPUT_ID}" }
		}],
		"fallback":  { "type":"model", "model":"${INPUT_ID}_in_hand" }
	},
	"swap_animation_scale": 1.95
}
